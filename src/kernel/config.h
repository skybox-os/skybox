/*
 *  SETTING:    IO_MODE
 *  DESCRIPTION:
 *      determines what type of input/output method shall be used
 *      for the terminal, if using terminal mode, and not in a
 *      graphics environment.
 *
 *  OPTIONS:
 *      * VGA_TEXT_MODE ........ Use VGA text mode for terminal
 *      * SERIAL_TERMINAL ...... Use serial as a terminal
 */
#define SERIAL_TERMINAL // IO_MODE

/*
 *  UNCOMMENT `#define DEBUG_MODE` IF YOU DON'T WANT TO RECIEVE DEBUG MESSAGES.
 */
#define DEBUG_MODE